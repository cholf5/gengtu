# Open Meme — Claude collaboration notes

## Workflow

- **不要主动 `git commit`。** 合作模式是：Claude 改代码 → 用户验收 → 用户明确说"提交"或"commit"后才执行 `git commit`。
- 改完一组改动只汇报"改了什么 / 测试结果"，等用户回复。
- 只有在用户用 `commit` / `提交` 之类的指令明确要求时，才执行 git 提交。
